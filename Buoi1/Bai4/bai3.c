#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a, b, c;
	a = 1;
	b = 2;
	c = 3;
	int d = 10 * a * a + 5 * b * c + c * c;
	printf("Ket qua d la: %d",d);
	return 0;
}
